{
  "permissions": {
    "allow": [
      "Bash(xcodebuild -scheme \"AI Tools\" -destination \"platform=macOS\" build)",
      "Bash(xcodebuild -scheme \"AI Tools\" -destination \"platform=macOS\" test)"
    ]
  }
}
